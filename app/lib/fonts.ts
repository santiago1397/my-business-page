import localFont from "next/font/local";

export const geistSans = localFont({
  variable: "--font-geist-sans",
  src: "../../public/fonts/geist-sans-latin.woff2",
  weight: "100 900",
  display: "swap",
});

export const geistMono = localFont({
  variable: "--font-geist-mono",
  src: "../../public/fonts/geist-mono-latin.woff2",
  weight: "100 900",
  display: "swap",
});

export const pixelSquare = localFont({
  variable: "--font-geist-pixel-square",
  src: "../../public/fonts/pixel-square.woff2",
  weight: "500",
  display: "swap",
});

export const pixelGrid = localFont({
  variable: "--font-geist-pixel-grid",
  src: "../../public/fonts/pixel-grid.woff2",
  weight: "500",
  display: "swap",
});

export const pixelCircle = localFont({
  variable: "--font-geist-pixel-circle",
  src: "../../public/fonts/pixel-circle.woff2",
  weight: "500",
  display: "swap",
});

export const pixelTriangle = localFont({
  variable: "--font-geist-pixel-triangle",
  src: "../../public/fonts/pixel-triangle.woff2",
  weight: "500",
  display: "swap",
});

export const pixelLine = localFont({
  variable: "--font-geist-pixel-line",
  src: "../../public/fonts/pixel-line.woff2",
  weight: "500",
  display: "swap",
});
